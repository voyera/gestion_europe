<h1><%= @unit.name %></h1>
<p>
  <%= link_to 'Edit', edit_unit_path(@unit), class: "btn btn-primary", role: "button" %>
  <%= link_to 'Back to Cooperative', @unit.cooperative, class: "btn btn-warning", role: "button" %>
</p>